#include <stdio.h>
/**
 * main- A program that prints and has a new line after it
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
